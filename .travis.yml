# R for travis:
# See documentation at https://docs.travis-ci.com/user/languages/r
# or https://github.com/metacran/r-builder for details.

language: R
sudo: false
cache: packages

notifications:
  email:
    on_success: never  # default: change
    on_failure: always # default: always
